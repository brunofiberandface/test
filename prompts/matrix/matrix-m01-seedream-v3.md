# M01 — Matrix-paint bottom-focus front view (Seedream) — v3 (lean)

## Purpose
Rev 23 of the matrix-paint M01 prompt. Mirrors M02 rev 28 — Bruno's
60-word lean prompt verbatim. Replaces v2 (rev 22) which had POSE CANON +
SILHOUETTE + LENGTH + HEM + STRICT REQUIREMENTS blocks. The lean approach
trusts visual evidence from the fit-model.front image to carry silhouette,
length, color, hem treatment.

## Architecture context
- IMAGE 1 = Tier-2 base (model in target shoes + placeholder pants), legsFront view.
- IMAGE 2 = fit-model straight front.
- No reference labels / inventory block prepended (seedream-client strips it).

## Placeholders
None. The lean prompt has no substitutions.

## Step 2 Prompt

```
Apply the fitmodel trousers onto the AI model with shoes. so merge the two images, but the trousers should be fully respected in form and fit. all details need to be preserved, IMPORTANT is the trousers run naturally down respecting the fitmodel lenght of the trousers. the model needs to stand equally on 2 feet, 50% on each foot, keeping the same position as the underwear model.
```
